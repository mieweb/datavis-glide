WC DataVis is a library for data visualization with presentation via
charts and tables (or as we call them, graphs and grids). The
architecture is a modular pipeline: user input can modify the data
produced by a source, which is manipulated by a view, to be shown by an
output module.
